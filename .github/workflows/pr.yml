name: On Pull Request

on:
  pull_request:
    branches:
      - master
      - development
      - release/*

jobs:
  test:
    uses: ./.github/workflows/test.yml
  pack:
    needs: [test]
    runs-on: ubuntu-latest

    steps:
    - name: Skip
      run: echo -e 'Skip Requirement'
