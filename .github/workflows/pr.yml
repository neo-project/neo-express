name: On Pull Request

on:
  pull_request:
    branches:
      - master
      - development
      - release/*
    paths-ignore:
      - '.gitignore'
      - '.gitattributes'
      - 'LICENSE'
      - '**/*.md'

jobs:
  test:
    name: Test Application
    uses: ./.github/workflows/test.yml
